请将下面的分段阅读笔记整合为一份 DeepPaperNote 风格的完整中文 Markdown 论文精读笔记。

输出结构必须包含以下二级章节：

# 论文标题

标题可以比原论文标题更适合中文读者阅读，但标题中的机构、模型名、对比对象、数字和结论必须来自原文证据。

## 核心信息
- 原文标题: 必须填写原文标题，保持英文原文
- 中文标题:
- 作者:
- 机构:
- 发表时间:
- 会议 / 期刊:
- DOI:
- 论文链接:
- 代码链接:
- 项目页:
- 领域:
- 论文类型:

只输出原文明确出现的字段；没有证据的字段不要写。

## 摘要
优先使用原文摘要证据，忠实写成中文摘要，不要加入评价。

## 背景与问题
用 2 到 4 个自然段讲清楚研究背景、任务重要性、已有方法不足和本文具体解决的问题。只使用摘要、引言、任务定义或相关工作中有证据支持的信息。

## 创新点
用 3 到 5 个短段落说明真实创新点。每个创新点说明它解决什么问题、为什么重要。

## 一句话总结
回答这篇论文真正解决什么问题；只写原文直接提到或由原文证据直接支持的内容。

## 数据与任务定义
说明输入、任务、数据集、评价指标和实验边界。原文没有提及的项目直接省略。

## 方法主线
必须包含 `### 机制流程`，用 3 到 5 步解释 Input -> 关键变换 -> Output。架构图、流程图、方法框架图放到本节对应解释段落附近。

必须包含 `### 关键公式`，解释 1 到 3 个最重要公式；如果有公式截图，正文只写公式编号、变量含义和工程作用，把对应 `[[ASSET:id]]` 放在公式解释附近，不要复写公式表达式。

## 关键结果
提炼最重要的指标、对比、消融和失败/边界证据。实验图、结果表、对比表、消融表和 case analysis 图放到本节对应解释段落附近。

## 深度分析
说明原文直接支持的贡献、有效性证据、证据薄弱处和作者明确讨论的假设；不要补写原文没有提及的推测。

## 局限
写真实局限，包括数据、评价、部署、复杂度、泛化或基线不足。

## 总结
收束全文，说明原文直接支持的结论和复现时应关注的实验点。

图表占位符规则：

- 只能使用可用图表截图中列出的 `[[ASSET:id]]`。
- 每个占位符必须独占一行。
- 占位符要放在解释该图/表/公式的段落旁边。
- 不要创建独立的 `## 图表精读` 章节。
- 不要为了插图重复同一占位符。
- 如果截图和正文解释关系不清楚，可以不使用它，不能编造解释。

格式规则：

- 不要输出 `<think>` 或任何思考过程。
- 不要输出 LaTeX 块公式、行内公式、等式、arg max/min、求和式或长变量表达；公式本体由截图展示，正文只写中文解释。
- 不要输出 markdown 表格；表格内容用自然段概括。
- 不要输出“翻译”二字；核心信息里写“中文标题”，摘要章节标题只写“摘要”。
- 不要输出 `## 引用` 或 `## 我的笔记` 章节。
- 除 `## 核心信息` 外，不要让大多数行以 `-` 开头。
- 原文没有提及的字段、章节和小节直接省略，不要写“未知”“N/A”等占位内容。
